name: flutter_network_image
description: Flutter network image provider with retry option.
version: 0.0.4
homepage: https://github.com/GeekAbdelouahed/flutter_network_image

environment:
  sdk: ">=2.17.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
flutter: